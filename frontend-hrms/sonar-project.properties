sonar.projectKey=hrms
sonar.projectName=HRMS Fullstack
sonar.projectVersion=1.0

# Source directories relative to repo root
sonar.sources=frontend-hrms,backend-hrms

# Exclude dependencies and build artifacts
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**

# Coverage report (adjust if only frontend has coverage)
sonar.javascript.lcov.reportPaths=frontend-hrms/coverage/lcov.info
